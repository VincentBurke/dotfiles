# README.md

This repo is a chezmoi source directory.

## Quick start (new Mac)

Run this in Terminal:

```sh
xcode-select --install
git clone https://github.com/VincentBurke/dotfiles.git ~/.dotfiles
cd ~/.dotfiles; source setup.sh
